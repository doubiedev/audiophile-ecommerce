import "tailwindcss";

const config = {
    theme: {
        extend: {
            fontFamily: {
                manrope: ['Manrope', 'sans-serif'],
            },
        },
    },
    plugins: [],
};

export default config;
